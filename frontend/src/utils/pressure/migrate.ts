import { MonthNoID } from '../../../../backend/src/types/pressure';

export const formatData = (migrateData: any): MonthNoID[] => {
  const formatedData: MonthNoID[] = migrateData;
  return formatedData;
};
