export interface Filter {
    group: string;
    startcharacter: string;
    name: string;
};

export interface TrackToPlay {
    id: string;
    path: string;
    artist: string;
    title: string;
};

